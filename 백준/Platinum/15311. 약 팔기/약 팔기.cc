#include <iostream>

#define endl "\n"

using namespace std;
int N, M;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(0);

    int a; cin >> a;

    cout << 2000 << endl;
    for (int i = 0; i < 999; i++) cout << 1 << " ";
    for (int i = 0; i < 1001; i++) cout << 1000 << " ";

}