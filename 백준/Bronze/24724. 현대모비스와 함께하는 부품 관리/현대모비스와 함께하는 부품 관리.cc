#include <iostream>

using namespace std;


int main(){
    
    int T, N, la, lb, A, B;
    cin >> T;
    for(int i = 1; i <= T; i++){
        cin >> N >> la >> lb;
        for(int j = 0; j < N; j++){
            cin >> A >> B;
        }
        cout << "Material Management " << i << '\n';
        cout << "Classification ---- End!\n";
    }
    
}